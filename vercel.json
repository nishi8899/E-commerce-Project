

{
    "buildCommand": "cd client && npm install --f && npm run build",
    "outputDirectory": "client/build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/server.js"
        }
    ]
}
